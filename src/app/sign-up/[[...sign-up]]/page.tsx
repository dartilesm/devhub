import { MockSignUp } from "@/components/mock-auth/mock-sign-up";

export default function SignUpPage() {
  return (
    <div className='flex min-h-[calc(100vh-4rem)] items-center justify-center'>
      <MockSignUp />
    </div>
  );
}
